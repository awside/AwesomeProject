import React from 'react'
import styled from 'styled-components/native'

export function Line(props: { width: number; length: number }) {
  return <Wrapper></Wrapper>
}

const Wrapper = styled.View``
